import { StyleSheet } from 'react-native';
import { Fonts } from '../../Resources/Fonts';

export const homeStyles = (appData) => {
  return StyleSheet.create({
    textStyle: {
      marginVertical: 20,
    },
  });
};
